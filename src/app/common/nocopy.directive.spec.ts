import { NocopyDirective } from './nocopy.directive';

describe('NocopyDirective', () => {
  it('should create an instance', () => {
    const directive = new NocopyDirective();
    expect(directive).toBeTruthy();
  });
});
